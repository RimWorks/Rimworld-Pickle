name: ship-list

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    uses: RimWorks/mod-ci/.github/workflows/ship-list.yml@eae5a8f32bee86128ae88bf8821ebaca65ddc1e9 # v1.4.1
    with:
      # the checker is fetched by npx at runtime, so this has to be a sha too, not a tag
      ref: eae5a8f32bee86128ae88bf8821ebaca65ddc1e9 # v1.4.1
